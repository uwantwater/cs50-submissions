#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(int argc, string argv[])
{
    string plaintext = get_string("plaintext: ");
    string text = strcat(argv, plaintext);
    printf("%s\n", text);
}