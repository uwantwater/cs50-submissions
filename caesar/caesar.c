#include <cs50.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, string argv[])
{
    int key = atoi(argv[1]);
    string plaintext[] = get_string("plaintext:  ")
    printf("%s\n", strcat(argv[1], plaintext));
}